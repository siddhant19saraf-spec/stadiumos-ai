# =============================================================================
# StadiumOS AI — Environment Variables
# Copy this file to .env and fill in your values. Never commit .env to git.
# =============================================================================

# --- AI Provider Keys (server-side only — NOT exposed to browser) ---

# OpenAI API key for AI Copilot features (server-side only)
# WARNING: Do NOT use NEXT_PUBLIC_ prefix — API keys must never reach the browser.
# For client-side AI features, the mock provider is used by default.
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google Gemini API key (server-side only)
# Get yours at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Backend API ---

# URL of the StadiumOS backend API
# Default: http://localhost:8000 (development)
# Production: https://your-backend.vercel.app or your deployed backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# --- Authentication ---

# Secret key for JWT signing — generate with: openssl rand -base64 32
# Required for authentication features
AUTH_SECRET=

# --- Logging ---

# Logging verbosity level
# Values: debug | info | warn | error
# Default: info
NEXT_PUBLIC_LOG_LEVEL=info
