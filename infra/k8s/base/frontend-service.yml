apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: stadiumos
spec:
  type: ClusterIP
  selector:
    app: stadiumos
    component: frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
